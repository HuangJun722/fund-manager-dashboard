{
  "name": "fund-manager-private-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Private fund management dashboard with daily quote cache and GitHub Pages publishing.",
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build_site.js",
    "sync:quotes": "node scripts/sync_quotes.js",
    "check": "node scripts/check_html.js"
  },
  "engines": {
    "node": ">=20"
  }
}
